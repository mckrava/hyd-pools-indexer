import calls from './calls';
import events from './events';
import storage from './storage';
import { ParserMethods } from '../../types/common';

export default { calls, events, storage } as ParserMethods;
